query GetUsersWithNotes($nom: String!) {
  utilisateur(where: { pseudonyme: { _eq: $nom } }) {
    pseudonyme
    notes_utilisateurs {
      note {
        notes_utilisateurs {
          utilisateur {
            pseudonyme
          }
        }
      }
    }
  }
}
