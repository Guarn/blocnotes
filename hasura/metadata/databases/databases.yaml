- name: bloc
  kind: postgres
  configuration:
    connection_info:
      database_url: postgresql://postgres:a@Postgres:5432/bloc
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include bloc/tables/tables.yaml"
